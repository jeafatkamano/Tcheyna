# ─── URL de l'API Flask ─────────────────────────────────────
# En développement : laisser vide ou commenté.
# Vite relaie automatiquement /api vers http://localhost:5000 (voir vite.config.ts).
#
# En production (Render) : renseigner l'URL complète de l'API.
# VITE_API_URL=https://tcheyna-api.onrender.com/api
